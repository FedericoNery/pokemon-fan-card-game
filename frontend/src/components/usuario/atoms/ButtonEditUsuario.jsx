import { Button } from "@mui/material";
import React from "react";

const ButtonEditUsuario = () => {
    return <Button
        type="submit"
        fullWidth
        variant="contained"
        sx={{ mt: 3, mb: 2 }}
    >
        Actualizar Usuario
    </Button>
}

export default ButtonEditUsuario;