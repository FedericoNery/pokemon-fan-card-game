import { Button } from "@mui/material";
import React from "react";

const BotonActualizarCartas = ({ onClick }) => {
    return <Button variant="contained" onClick={onClick}>Actualizar</Button>
}
 
export default BotonActualizarCartas;